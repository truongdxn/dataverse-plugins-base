{
  "$comment": [
    "Cache of SdkMessage name -> sdkmessageid, required by 'dv pack'.",
    "A packed solution references messages by GUID only, so the ids must be known offline.",
    "",
    "Populate once with:  dv messages pull -e dev",
    "then COMMIT this file. After that packing needs no environment at all."
  ],
  "messages": {}
}
