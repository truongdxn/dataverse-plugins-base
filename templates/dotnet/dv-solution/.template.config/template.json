{
  "$schema": "http://json.schemastore.org/template",
  "author": "Dataverse plugin base",
  "classifications": [
    "Dataverse",
    "Solution",
    "Power Platform"
  ],
  "identity": "Dataverse.Plugins.Solution",
  "name": "Dataverse solution folder",
  "description": "A new PowerApps solution: its own publisher, schema snapshot, message cache and .zip. Create it under CRM/Plugins, then add assembly projects inside it.",
  "shortName": "dv-solution",
  "tags": {
    "type": "project"
  },
  "sourceName": "DvSolution",
  "preferNameDirectory": true,
  "symbols": {
    "publisherPrefix": {
      "type": "parameter",
      "datatype": "string",
      "defaultValue": "dv",
      "replaces": "DV_PREFIX",
      "description": "Publisher prefix on custom components, e.g. 'contoso'. Two to eight letters."
    },
    "optionValuePrefix": {
      "type": "parameter",
      "datatype": "integer",
      "defaultValue": "55973",
      "replaces": "55973",
      "description": "Numeric block new option set values are allocated from. Unique per publisher."
    }
  }
}
